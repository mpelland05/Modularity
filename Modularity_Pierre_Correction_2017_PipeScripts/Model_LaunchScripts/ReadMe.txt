Control scripts: 
Controls scripts are scripts were originally used to make sure the method worked. It was used to
randomly sample a defined number of participants from a large pool, randomly divide the sample into
two groups and test wether the algorithm would find difference between these two made up groups. 
This is done repeatedly in a bootstrap fashion. 

Normal test scripts:
These scripts should be used when one desires to compare the clustering solution of two groups.